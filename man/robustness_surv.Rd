% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/robustness_models.R
\name{robustness_surv}
\alias{robustness_surv}
\title{Robustness analysis for a Cox proportional hazards term}
\usage{
robustness_surv(
  formula,
  data,
  term,
  alpha = 0.05,
  n_boot = 1000,
  max_removal_pct = 0.3,
  weights = c(jackknife = 0.4, fragility = 0.4, bootstrap = 0.2),
  seed = 123
)
}
\arguments{
\item{formula}{Survival formula, e.g. survival::Surv(time, event) ~ arm + age}

\item{data}{Data frame (one row per subject)}

\item{term}{Term whose p-value defines the conclusion. Either:
\itemize{
\item a single coefficient row name from \code{summary(coxph(...))$coefficients}
(e.g. \code{"armActive"}), tested with the Wald z p-value; or
\item a multi-df term label from \code{attr(terms(coxph(...)), "term.labels")}
(e.g. a 3-level factor \code{"arm"}), tested jointly via
\code{drop1(..., test = "Chisq")} (likelihood-ratio test; survival S3 method).
For multi-df terms the stored \code{estimate} is \code{NA} and \code{term_info} records
the joint LRT (ndf / statistic).
}}

\item{alpha, n_boot, max_removal_pct, weights, seed}{As in \code{\link[=robustness_analysis]{robustness_analysis()}}}
}
\value{
An object of class \code{"robustness_model"} (a named list). Same engine
fields as \code{\link[=robustness_lm]{robustness_lm()}} (\code{original_p}, \code{metrics},
\code{interpretation_label}, jackknife / worst-case / bootstrap tibbles, plus
\code{term}, \code{term_info}, \code{model}, \code{type}, and related metadata).
}
\description{
Robustness analysis for a Cox proportional hazards term
}
\details{
Single-coefficient \code{term} strings keep the previous Wald behaviour.
Multi-df factors are detected when \code{term} matches a term label that expands
to more than one non-aliased design-matrix column (same rules as
\code{\link[=robustness_lm]{robustness_lm()}}). Case deletion/resampling acts on subjects, so censoring
patterns are handled naturally. Removing whole subjects (with their
follow-up) differs from the event-flip fragility of Walsh et al.; interpret
as a removal fragility index.

Robustness calculations use exactly the rows retained by the full fitted
model after its \code{na.action}. At least one further row must be removable
while retaining the engine's minimum analysis size.
Numeric scores and component metrics remain available for every Cox
result. The \code{cox_ph} calibration unit is currently uncalibrated, so its
categorical label is suppressed (\code{NA}) even when the term is significant.
}
\examples{
if (requireNamespace("survival", quietly = TRUE)) {
  set.seed(1)
  dat <- data.frame(
    time = rexp(30, 0.1),
    event = rbinom(30, 1, 0.8),
    arm = factor(rep(c("A", "B"), each = 15)),
    age = rnorm(30, 60, 8)
  )
  res <- robustness_surv(survival::Surv(time, event) ~ arm + age, dat,
                         term = "armB", n_boot = 15, seed = 1)
  print(res)
}
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/robustness_models.R
\name{robustness_lm}
\alias{robustness_lm}
\title{Robustness analysis for a linear model / ANCOVA term}
\usage{
robustness_lm(
  formula,
  data,
  term,
  alpha = 0.05,
  n_boot = 1000,
  max_removal_pct = 0.3,
  weights = c(jackknife = 0.4, fragility = 0.4, bootstrap = 0.2),
  seed = 123
)
}
\arguments{
\item{formula}{Model formula, e.g. \code{change ~ arm + baseline}}

\item{data}{Data frame (one row per subject)}

\item{term}{Term whose p-value defines the conclusion. Either:
\itemize{
\item a single coefficient row name from \code{summary(lm(...))$coefficients}
(e.g. \code{"armActive"}), tested with the usual coefficient t / Wald p-value; or
\item a multi-df term label from \code{attr(terms(lm(...)), "term.labels")}
(e.g. a 3-level factor \code{"arm"}), tested jointly via
\code{drop1(..., test = "F")}. For multi-df terms the stored \code{estimate} is
\code{NA} and \code{term_info} records the joint F test (ndf / statistic).
}}

\item{alpha, n_boot, max_removal_pct, weights, seed}{As in \code{\link[=robustness_analysis]{robustness_analysis()}}}
}
\value{
An object of class \code{"robustness_model"} (a named list) with:
\describe{
\item{original_p, original_estimate, original_significant}{Full-data
term test (\code{estimate} is \code{NA} for multi-df joint tests).}
\item{metrics}{Tibble of jackknife / worst-case fragility / bootstrap
component scores and the overall composite. Alias:
\code{robustness_metrics} (same tibble). Shared metric columns match
\code{\link[=robustness_analysis]{robustness_analysis()}} where meanings align.}
\item{interpretation_label}{A calibrated categorical label when a
method-specific calibration is applicable; otherwise \code{NA}. Scores and
component metrics are retained when the label is suppressed. Alias:
\code{robustness_interpretation}.}
\item{calibration}{Method-specific calibration metadata, including
applicability, status, cutoffs, version, and provenance.}
\item{original_estimate}{Term estimate (\code{NA} for multi-df joint tests).
Alias: \code{original_mean_diff}.}
\item{jackknife, worstcase, bootstrap, removed_rows}{Component analysis
tibbles and the greedy-removal path (flat tibbles; nesting differs
from \code{\link[=robustness_analysis]{robustness_analysis()}}).}
\item{term, term_info, sample_info, model, type, n, max_k, alpha,
max_removal_pct, weights}{Model and analysis metadata.}
}
}
\description{
Robustness analysis for a linear model / ANCOVA term
}
\details{
Single-coefficient \code{term} strings keep the previous Wald/t
behaviour. Multi-df factors are detected when \code{term} matches a term label
that expands to more than one non-aliased design-matrix column. Ambiguous
matches error. Conclusion for the robustness pipeline is \code{p < alpha}
(joint F p-value for multi-df terms).

Robustness calculations use exactly the rows retained by the full fitted
model after its \code{na.action}. At least one further row must be removable
while retaining the engine's minimum analysis size; otherwise the function
raises an insufficient-sample error rather than returning an unevaluated
fragility score.
Numeric scores and component metrics remain available for every ANCOVA
result. The \code{lm_ancova} calibration unit is currently uncalibrated, so its
categorical label is suppressed (\code{NA}) even when the term is significant.
}
\examples{
set.seed(1)
dat <- data.frame(
  change = c(rnorm(15, -5), rnorm(15, 0)),
  arm = factor(rep(c("Placebo", "Active"), each = 15),
               levels = c("Placebo", "Active")),
  baseline = rnorm(30, 50, 10)
)
res <- robustness_lm(change ~ arm + baseline, dat,
                     term = "armActive", n_boot = 25, seed = 1)
print(res)

# Multi-df factor: joint F via drop1()
res_joint <- robustness_lm(change ~ arm + baseline, dat,
                           term = "arm", n_boot = 25, seed = 1)
}
